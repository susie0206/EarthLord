# 删除账户功能使用说明

## ✅ 已实现功能

### 1. **后端 - Supabase 边缘函数** ✅
- **函数名称**: `delete-account`
- **端点**: `https://absexnnamqwkqedaaamt.supabase.co/functions/v1/delete-account`
- **状态**: ACTIVE

### 2. **前端 - AuthManager.swift** ✅
- **方法**: `deleteAccount() async`
- **功能**: 调用边缘函数删除用户账户
- **日志**: 详细的中文调试日志

### 3. **UI - ProfileTabView.swift** ✅
- **位置**: 个人页面底部
- **组件**: 删除账户按钮 + 确认对话框
- **验证**: 需要输入"删除"文字确认

## 📱 功能界面

### 个人页面结构

```
┌────────────────────────────────┐
│   头部背景（渐变）              │
├────────────────────────────────┤
│   用户信息卡片                  │
│   - 头像                        │
│   - 用户名                      │
│   - 邮箱                        │
│   - 在线状态                    │
├────────────────────────────────┤
│   幸存者数据统计                │
│   [领地] [探索点] [资源]        │
├────────────────────────────────┤
│   设置功能                      │
│   - 编辑资料                    │
│   - 通知设置                    │
│   - 隐私设置                    │
│   - 帮助与反馈                  │
├────────────────────────────────┤
│   ┌──────────────────────┐    │
│   │   退出登录            │    │
│   └──────────────────────┘    │
├────────────────────────────────┤
│   ⚠️ 警告文本                   │
│   "删除账户后，所有数据将被      │
│    永久删除且无法恢复"          │
│                                │
│   ┌──────────────────────┐    │
│   │  🗑️ 删除账户         │    │
│   └──────────────────────┘    │
│   （红色，危险样式）            │
└────────────────────────────────┘
```

### 确认对话框

```
┌────────────────────────────────┐
│        删除账户                 │
├────────────────────────────────┤
│  ⚠️ 此操作不可逆！              │
│                                │
│  删除后，您的所有领地、资源和    │
│  探索记录都将永久丢失。          │
│                                │
│  请输入"删除"来确认此操作。      │
├────────────────────────────────┤
│  ┌──────────────────────────┐ │
│  │ 请输入"删除"以确认        │ │
│  └──────────────────────────┘ │
├────────────────────────────────┤
│  [ 确认删除 ]    [ 取消 ]      │
│  （只有输入"删除"才可点击）     │
└────────────────────────────────┘
```

## 🔍 使用流程

### 步骤 1: 导航到个人页面
1. 打开应用
2. 点击底部导航栏的"个人" Tab
3. 滚动到页面底部

### 步骤 2: 点击删除账户按钮
1. 看到红色的"删除账户"按钮
2. 上方有警告文本提示
3. 点击按钮

**控制台日志：**
```
🔵 [UI] 用户点击了删除账户按钮
```

### 步骤 3: 确认对话框
弹出确认对话框，显示：
- 标题："删除账户"
- 警告信息
- 输入框："请输入"删除"以确认"
- 两个按钮："确认删除"（红色）、"取消"

### 步骤 4: 输入确认文本
1. 在输入框中输入：`删除`
2. "确认删除"按钮变为可点击状态
3. 点击"确认删除"

**控制台日志：**
```
🔵 [UI] 用户确认删除账户
   用户输入的确认文本: '删除'
✅ [UI] 确认文本匹配，开始删除账户
```

### 步骤 5: 删除执行
系统开始执行删除流程：

**控制台日志（AuthManager）：**
```
🔵 [删除账户] 开始删除账户流程
🔵 [删除账户] 步骤1: 获取当前会话
✅ [删除账户] 会话获取成功
   用户ID: xxx
   用户邮箱: xxx@example.com
🔵 [删除账户] 步骤2: 调用 delete-account 边缘函数
✅ [删除账户] 边缘函数调用成功
   响应消息: 账户已成功删除
   删除的用户ID: xxx
🔵 [删除账户] 步骤3: 清空本地状态
✅ [删除账户] 本地状态已清空
🎉 [删除账户] 账户删除流程完成！
```

**边缘函数日志：**
```
🔵 [删除账户] 开始处理删除账户请求
🔵 [删除账户] 检查 Authorization header: 存在
✅ [删除账户] JWT token 已提取
🔵 [删除账户] 创建用户验证客户端
🔵 [删除账户] 验证用户身份
✅ [删除账户] 用户身份验证成功
🔵 [删除账户] 创建管理员客户端
🔵 [删除账户] 开始删除用户账户
✅ [删除账户] 用户账户删除成功
🎉 [删除账户] 账户删除流程完成！
```

### 步骤 6: 自动登出
- 账户删除成功后
- 本地状态自动清空
- 用户被引导回登录页面

## 🔒 安全特性

### 1. **双重确认**
- ✅ 需要用户手动输入"删除"文字
- ✅ 只有输入完全匹配才能点击确认按钮
- ✅ 防止误操作

### 2. **身份验证**
- ✅ 必须是已登录用户
- ✅ 使用 JWT token 验证身份
- ✅ 只能删除自己的账户

### 3. **权限控制**
- ✅ 边缘函数使用 service_role key
- ✅ 客户端无法直接访问管理员 API
- ✅ 通过边缘函数中转，确保安全

### 4. **日志记录**
- ✅ UI 层记录用户操作
- ✅ AuthManager 记录删除流程
- ✅ 边缘函数记录服务器端操作
- ✅ 方便问题追踪和审计

## ⚠️ 重要注意事项

### 1. 数据删除范围

**已删除：**
- ✅ `auth.users` 表中的用户记录

**需要配置级联删除：**
- ⚠️ `profiles` 表中的用户资料
- ⚠️ `territories` 表中的领地数据
- ⚠️ `pois` 表中的探索记录
- ⚠️ 其他关联数据

### 2. 配置数据库级联删除

**必须执行以下 SQL：**

```sql
-- 在 profiles 表上设置级联删除
ALTER TABLE profiles
DROP CONSTRAINT IF EXISTS profiles_id_fkey,
ADD CONSTRAINT profiles_id_fkey
  FOREIGN KEY (id)
  REFERENCES auth.users(id)
  ON DELETE CASCADE;

-- 在 territories 表上设置级联删除
ALTER TABLE territories
DROP CONSTRAINT IF EXISTS territories_user_id_fkey,
ADD CONSTRAINT territories_user_id_fkey
  FOREIGN KEY (user_id)
  REFERENCES auth.users(id)
  ON DELETE CASCADE;

-- 在 pois 表上设置（根据业务需求选择 CASCADE 或 SET NULL）
ALTER TABLE pois
DROP CONSTRAINT IF EXISTS pois_discovered_by_fkey,
ADD CONSTRAINT pois_discovered_by_fkey
  FOREIGN KEY (discovered_by)
  REFERENCES auth.users(id)
  ON DELETE SET NULL;  -- 或使用 CASCADE
```

### 3. 用户体验建议

**当前实现：**
- ✅ 二次确认（输入文字）
- ✅ 警告提示
- ✅ 红色危险样式

**未来改进：**
- 📧 发送确认邮件
- ⏰ 30天延迟删除（软删除）
- 📊 删除前允许数据导出
- 🔔 删除前再次确认重要数据

## 🧪 测试步骤

### 1. 准备测试账号
```bash
# 使用测试邮箱注册账号
# 例如：test-delete@example.com
```

### 2. 登录测试账号
1. 打开应用
2. 使用测试账号登录
3. 进入个人页面

### 3. 测试删除流程
**测试场景 1: 正常删除**
1. 点击"删除账户"按钮
2. 输入"删除"
3. 点击"确认删除"
4. 观察控制台日志
5. 验证账户是否被删除
6. 验证是否自动登出

**测试场景 2: 输入错误文本**
1. 点击"删除账户"按钮
2. 输入"delete"（错误）
3. 观察"确认删除"按钮是否禁用
4. 点击"取消"

**测试场景 3: 取消操作**
1. 点击"删除账户"按钮
2. 点击"取消"
3. 确认对话框关闭
4. 账户未被删除

### 4. 验证数据删除
1. 在 Supabase Dashboard 中检查：
   - `auth.users` 表 - 用户记录应该被删除
   - `profiles` 表 - 用户资料应该被删除（如果配置了级联）
   - `territories` 表 - 领地数据应该被删除（如果配置了级联）

### 5. 查看日志
在 Xcode 控制台中搜索：
- `[UI]` - UI 层日志
- `[删除账户]` - AuthManager 日志
- 在 Supabase Dashboard 查看边缘函数日志

## 🐛 常见问题

### 问题 1: 点击确认后没有反应
**可能原因：**
- 输入的文本不是"删除"
- 网络连接问题
- 边缘函数错误

**解决方案：**
1. 确认输入的是中文"删除"
2. 检查网络连接
3. 查看控制台错误日志

### 问题 2: 删除失败，返回错误
**可能原因：**
- 用户未登录
- JWT token 过期
- 边缘函数错误

**解决方案：**
1. 重新登录
2. 查看错误消息
3. 检查边缘函数日志

### 问题 3: 账户删除后关联数据未清理
**原因：**
- 未配置数据库级联删除

**解决方案：**
1. 执行上述 SQL 配置级联删除
2. 或在边缘函数中手动删除关联数据

## 📊 数据流图

```
用户操作
   ↓
点击"删除账户"按钮
   ↓
显示确认对话框
   ↓
输入"删除"确认
   ↓
调用 authManager.deleteAccount()
   ↓
获取当前会话 (JWT token)
   ↓
调用边缘函数
POST /functions/v1/delete-account
Authorization: Bearer <token>
   ↓
边缘函数验证 JWT
   ↓
边缘函数使用 service_role key
调用 admin.deleteUser()
   ↓
Supabase 删除用户
   ↓
触发数据库级联删除
   ↓
返回成功响应
   ↓
清空本地状态
   ↓
自动跳转到登录页面
```

## 📝 代码位置

### AuthManager.swift
**文件路径**: `EarthLord/Services/AuthManager.swift`
**方法**: `deleteAccount() async`
**行号**: 约 473-549 行

### ProfileTabView.swift
**文件路径**: `EarthLord/Views/Tabs/ProfileTabView.swift`
**状态变量**:
- `showDeleteConfirmation` (第 15 行)
- `deleteConfirmationText` (第 18 行)
**UI 组件**: `deleteAccountButton` (约 336-410 行)

### Supabase 边缘函数
**函数名**: `delete-account`
**函数 ID**: `ed7d95d1-df1d-4631-b613-9660c269b7e0`
**详细文档**: 见 `SUPABASE_DELETE_ACCOUNT_FUNCTION.md`

## 🔗 相关文档

- [Supabase 删除账户函数文档](./SUPABASE_DELETE_ACCOUNT_FUNCTION.md)
- [Supabase Edge Functions](https://supabase.com/docs/guides/functions)
- [Supabase Auth Admin API](https://supabase.com/docs/reference/javascript/auth-admin-deleteuser)

---

**创建时间**: 2025-12-30
**状态**: ✅ 已实现并可测试
**版本**: v1.0
